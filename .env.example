# ---- Bluverse LMS-II Frontend env (copy to .env for local dev) ----
# Vite inlines this at BUILD time. Point it at the backend API base URL.
# Local dev:
VITE_API_URL=http://localhost:5000
# Production example:
# VITE_API_URL=https://api.yourdomain.com
